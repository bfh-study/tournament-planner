{% extends 'base.html.twig' %}

{% block body %}
<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-3">Standing - {{ tournament.name }}</h1>
    </div>
</div>
<div class="container">
{{ form_start(form) }}
  <div class="form-row">
    <div class="col-6">
      {{ form_widget(form.tournament, {'attr': {'class': 'form-control'}}) }}
    </div>
  </div>
  <div class="form-row">
    <div class="col-6">
          {{ form_widget(form.generate, {'attr': {'class': 'btn btn-primary'}}) }}    
    </div>
  </div>
{{ form_end(form) }}
</div>
<br />
<div class="container">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Team</th>
                <th>Played</th>
                <th>Won</th>
                <th>Drawn</th>
                <th>Lost</th>
                <th>Goals</th>
                <th>Dif.</th>
                <th>Points</th>
            </tr>
        </thead>
        <tbody>
            {% for team in standings %}
                <tr>
                    <th scope="row">{{ loop.index }}</th>
                    <td>{{ team.name }}</td>
                    <td>{{ team.played }}</td>
                    <td>{{ team.won }}</td>
                    <td>{{ team.drawn }}</td>
                    <td>{{ team.lost }}</td>
                    <td>{{ team.goalsFor}}:{{team.goalsAgainst }}</td>
                    <td>{{ team.differenz }}</td>
                    <td>{{ team.points }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}